# ========================================
# Variables de entorno para docker-compose.multi.yml
# ========================================

# ============ DATABASE ============
DB_HOST=estacioncafedb
DB_PORT=5432
DB_USERNAME=admin
DB_PASSWORD=estacionPass2025
DB_NAME=estacioncafedb

# Para desarrollo local (sin Docker), usar:
# DB_HOST=localhost
# DB_PORT=5555

# ============ API ============
NODE_ENV=development
# En producción cambiar a: NODE_ENV=production

# JWT y seguridad (configurar en SECURITY_CREDENTIALS.env)
# JWT_SECRET=your_secret_key_here
# JWT_EXPIRES_IN=24h

# ============ FRONTEND ============
# URL de la API (dentro de Docker)
API_URL=http://estacioncafe_api:3484

# Para desarrollo local:
# API_URL=http://localhost:3484

# ============ ML SERVICE ============
# URL completa de conexión a PostgreSQL
DATABASE_URL=postgresql://admin:estacionPass2025@estacioncafedb:5432/estacioncafedb

# Para desarrollo local:
# DATABASE_URL=postgresql://admin:estacionPass2025@localhost:5555/estacioncafedb

# ============ POSTGRESQL ============
POSTGRES_USER=admin
POSTGRES_PASSWORD=estacionPass2025
POSTGRES_DB=estacioncafedb

# ========================================
# NOTAS:
# - Estas variables están configuradas directamente en docker-compose.multi.yml
# - Para producción, crear un archivo .env y referenciarlo en el compose
# - NUNCA commitear credenciales reales al repositorio
# ========================================
